[package]
name = "wasikv"
version = "0.1.0"
edition = "2021"

[dependencies]
tiny_http = "0.12"

# commented out because this is a standalone project, not a library
# [lib]
# crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
